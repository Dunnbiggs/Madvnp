<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # If the request is NOT for the dist folder already
  RewriteCond %{REQUEST_URI} !^/dist/
  
  # Rewrite the request to the dist folder
  RewriteRule ^(.*)$ dist/$1 [L]
</IfModule>
